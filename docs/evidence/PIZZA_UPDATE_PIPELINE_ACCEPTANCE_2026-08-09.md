# Pizza update pipeline acceptance checkpoint — 2026-08-09

Verdict: **FAIL / CLAUDE BRIDGE BLOCKED**

## Accepted prerequisite

The evidence-backed tracker reconciliation is complete on branch
`codex/homecoming-kai-claude-partnership` at commit `7da67235`. Its focused
delivery-box, portfolio, and Factory conveyor suite passed 37/37. The real
updated app remains running and responsive as PID 27260 from the governed
worktree. No restart, navigation, capture, Firebase action, or live-service
mutation occurred during Brief 019A.

## Labor/model gate

Persistent Claude session: `32011ace-4de7-4d54-959c-2170bf719a93`.

Three independent bridge checks produced no admissible model proof:

1. The callable partnership tool schema exposes no model identifier field.
2. The metadata-only response envelope exposed only `content` and `isError`;
   there was no transport `model`, structured metadata, or provider receipt.
3. Claude confirmed that its callable tool surface has no model-identity
   endpoint. Its prompt-supplied `claude-opus-5` label was explicitly excluded
   as self-description, not transport evidence.

Result: **CLAUDE MODEL UNVERIFIED / INELIGIBLE FOR CODING**. No Claude code was
requested or accepted after the gate. Claude-authored files/commit: none.

The later portfolio packet reports that a separate direct Claude session has
verified Opus 5. That does not prove the callable partnership relay's dispatched
model and does not supply write-enabled relay security authority, so it cannot
open this implementation gate.

## Claude adversarial review

Review handoff elapsed approximately 109 seconds; the metadata-only bridge check
elapsed 29.5 seconds. Claude edited no files.

Accepted findings, independently reproduced:

- capture kind/method were self-declared without an out-of-band binding;
- runtime/capture bound `Kai.exe` but not authoritative `data/app.so`;
- capture did not prove OS process creation after artifact binding;
- DPI-awareness failure was ignored;
- global luminance/yellow ratios did not prove per-label readability;
- UI Automation navigation was the mutating default;
- duplicate semantic labels could collapse in a name-keyed map;
- output/manifest/ledger paths were not root-constrained;
- retry suppression was optional and caller-controlled;
- prior failure fingerprints were not cycle-scoped.

Rejected Claude findings: none material. The schema file was not opened by
Claude and its review is `UNVERIFIED`; Codex froze and tested the schema contract
independently.

Duplicate/rework cost: zero duplicate implementation between agents (Claude was
read-only). Codex performed one repair pass on its pre-gate draft after the
review; the labor gate then stopped further Codex implementation. That draft is
preserved but unaccepted.

## Frozen acceptance suite

Command:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\test\test_kai_pizza_update_pipeline.ps1
```

Result: **4 PASS / 10 FAIL**.

Passing gates:

- valid machine-readable delta and deterministic focused-test selection;
- sponsor-owned verified promotion refusal;
- evidence path traversal refusal;
- no destructive process fallback in the capture draft.

Failing gates:

- unresolved Git object fixture handling is not stable;
- build source binding fails against the linked worktree, cascading into four
  build/runtime/capture fixture failures;
- non-mutating attended navigation is not the default;
- failure ledger and allowed output root are not mandatory;
- DPI-awareness setup does not fail closed;
- capture tool does not yet bind `app.so` and OS freshness.

First causal failure in the acceptance run: the expected missing-commit fixture
causes native `git` stderr (`fatal: Needed a single revision`) to escape as a
terminating error under the strict parent harness before the validator receipt
can be asserted. A separate read-only check proved that the valid current
worktree spec `7da67235…^{commit}` resolves successfully, so the linked worktree
is not the first cause. The harness must capture native exit/stdout/stderr as
data before assessing the validator. The subsequent build-source failure and
its four downstream runtime/capture failures remain a separate focused repair
packet rather than being inferred from the first cascade.

The implementation therefore remains **unaccepted**. A parser-only check passed
for both draft implementation scripts and the acceptance suite; syntax is not
behavioral acceptance.

## Preserved draft inventory

- `scripts/tools/kai_pizza_update_pipeline.ps1` — SHA-256
  `8ED2C970664BA333F8B3374B45D2E4BDCBB903BDDB965839CD0D64E475C14D03`.
- `scripts/tools/kai_real_pizza_capture.ps1` — SHA-256
  `78CB5A6846133C569046DCF7AD86BEC6702493B88AE906C3598BCCABBAA4A934`.
- `scripts/test/test_kai_pizza_update_pipeline.ps1` — SHA-256
  `7C49F81A5D135405C96A639E4C5A766DDA8BFF9AF938E3A45BCFBEAA1CD0A82B`.
- `docs/evidence/PIZZA_TRACKER_DELTA_2026-08-09.json` — SHA-256
  `4A74A96595A2EF0E9E6663B54C6E4E1C0A68164FD1A9AC894DBBCF2418A1A361`.
- `docs/evidence/pizza_tracker_delta.schema.json` — SHA-256
  `EAD77B7F7DA53EC5FD58724D5043BA5A67FAC0D47C346213BFED1463180E84D7`.

No draft file may be presented as accepted automation.

## Capture-cycle evidence

The accepted real-app capture was produced 438 seconds after PID 27260 started
(16:14:18 to 16:21:36 Bahrain). This is a cycle-time proxy, not a fully
instrumented pipeline duration.

Distinct failed-capture fingerprints retained:

- failed synthetic/block-glyph QA:
  `521DF78D90191E1E47A9692A2AE8BB25EFDCD4AD396912DE88AC0CF57BAC5275`;
- synthetic readable attempt:
  `805C110DA13D5AEB41DAE733A2F387AA2CC3D2C1FE28465175400FCA01CE4C4C`;
- synthetic readable V2:
  `7EC0007F9187DD06C86A9A3787BBAEFFE8FF48468CC1D3124EAE75E8CCDF00E1`;
- initial real window:
  `CF3D187C6F870D01EB103A90D2CF0196C3AC8E973AC1C85A087BB2F6D393367A`;
- Unity-contaminated capture:
  `0D35456D5A7D31948735ED3F947A685430F8B2F46562485BED240A781593025D`;
- direct clipped capture:
  `C4DA446972D101BF769F635C7E71C701736507CDFC2F0EF9A9892601CC7E6147`;
- Hoard selection variants:
  `5D6F9606345F8213FA355F4EA110836B0A505FA0FDA7C569E322F3B3E896E3EE`
  and `5458BFB4371841BEE2D9BFA651600CFACF9AAF654CA51806769F76A69BEDA19A`;
- blank DPI attempt:
  `BB66660C4A6190EFDCE3AE4AC3A5B0B3EF97B5AD30FB22AB7D750469B986D3B5`.

Accepted real direct-window capture:
`945722F65A03AB0F4E070963F7B09F79DB8A87DA869E75697C1024DBF50BF384`.

DPI requirement remains: capture process awareness must be set and verified
before any geometry API; client-space label rectangles must be checked at the
target window's actual DPI. The draft does not yet pass that gate.

## Manual steps and sponsor gates

Manual steps targeted for removal—evidence transcription, test selection,
artifact hash binding, PID/path/time capture, DPI lookup, direct-window capture,
label geometry checks, and failure fingerprinting—remain **planned, not removed**
because the pipeline is not accepted.

Current gates:

- Opus 5+ transport-reported model identity is required before Claude coding.
- A current explicit authority ID is required for any process handover, restart,
  GUI navigation, or direct-window capture.
- Graceful room and coordinator exits must be product-supported; no force-kill
  fallback exists.
- Synthetic/test-rendered images remain ineligible for real-desktop proof.
- The newly reconciled tracker delta has no fresh real-app screenshot because
  restart/navigation/capture were forbidden; its screenshot state is
  `UNVERIFIED`, and the earlier accepted PNG must not be reused as new proof.

## 13:23–13:53 portfolio delta binding

- Factory commit `b6602c668fc670413cce3865eb6b71949fbb0e89` resolves locally
  on `codex/factory-operator-automation`. Primary files add the venue-scale
  decision model, investment memo, sensitivity analysis, and focused tests.
  The memo recommends `INVEST` under a 20-hour/BHD0 local MVP ceiling but says
  separate exact Assembly authority is mandatory. Classification: Blueprint
  packet `TESTED`; Assembly/public `FUTURE`.
- The reported 37/37 Factory result is retained as supplied evidence; no second
  broad test run was performed in this PM-only checkpoint.
- Kingdom K1.6 remains active but code proof does not advance. Hoard retains its
  live recovery gate and Brief 014 PM/tests. BoothSignal remains Tested,
  package-ready, and unpublished. TikTok remains on local-safe media work.
- Homecoming receives no phase promotion. The new delta is preserved in
  `PIZZA_TRACKER_DELTA_2026-08-09.json`; code integration and screenshot are
  `UNVERIFIED`.

## Exact next step

Provide relay/tool transport metadata proving Claude Opus 5+ and dispatch the
frozen Brief 019A plus acceptance suite to an isolated Claude-owned file set.
If that model proof remains unavailable, sponsor authorization is required for
a Codex coding fallback. Until one of those gates opens, preserve the draft and
do not touch PID 27260.

## H019A-3 independent rerun

The later verified Opus 5 implementation lane produced H019A-3 on the same
one-file scope. Codex reran the unchanged frozen suite independently.

Result: **9 PASS / 5 FAIL — REJECTED**.

Pipeline-owned failure:

- Case 7, `real capture fixture is TEST_ONLY and needs an out-of-band pin`,
  terminates at `scripts/tools/kai_pizza_update_pipeline.ps1:138` because
  `.Contains($Name)` is called on a dictionary shape without a compatible
  one-argument overload. The validator returns `unhandled_exception` rather
  than the expected deterministic PASS receipt.

Capture-script failures retained for Brief 019B:

- case 10: attended-bound navigation is not the default;
- case 11: failure ledger and allowed output root are not mandatory;
- case 12: DPI-awareness failure is not fail-closed;
- case 13: capture does not bind `app.so` and process freshness.

Case 7 prevents the required 10/14 H019A boundary, so Brief 019B remains
locked. H019A-4 is bounded to dictionary key lookup and canonical capture
binding handling in `scripts/tools/kai_pizza_update_pipeline.ps1` only.
