<!-- Kai's status-bar notification icon: the P5 lightning bolt.
     Android tints small notification icons to a flat white silhouette using
     only the alpha channel, so this is a solid white shape on transparent —
     colour here is irrelevant, only the outline reaches the status bar.
     The same bolt is drawn in kai_p5_chat.dart's bubble tail. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M13,2 L4.5,13.2 L11,13.2 L9.5,22 L19.5,10.2 L12.8,10.2 Z" />
</vector>
